# Simulator notes

This folder is intentionally minimal. The browser-based simulator in the repo root is the live visual tool, while the MakeCode package layout lets you adapt the same code to the micro:bit MakeCode environment.

The main goal is to support the same API names used by 4tronix BitBot, including drive commands and basic sensor placeholders.
